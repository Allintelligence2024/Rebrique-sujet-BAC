// Aggregate, pseudonymized P2 usability evidence. Never add names, contacts, audio, or raw consent forms here.
export const P2_USABILITY_STUDY = Object.freeze({
  protocolVersion: 1,
  requiredParticipants: 5,
  sessions: Object.freeze([])
});
